Section "Device"
    Identifier "NVIDIA Card"
    Driver     "nvidia"
    Option     "NoLogo" "1"  # Skip splash
EndSection

# User overrides — add/edit in /etc/X11/xorg.conf.d/20-nvidia.conf (we symlink only if missing)
