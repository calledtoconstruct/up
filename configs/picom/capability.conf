# Default compositor capability fragment (overwritten by apply-compositor-profile.sh).
# Kept in-tree so @include "capability.conf" resolves before first detection.
# Conservative defaults: safe path until detection runs.

backend = "xrender";
xrender-sync-fence = false;
blur: {
    method = "none";
};
blur-background = false;
