# Extra i3 configuration. up-update never overwrites this file.
# Included last from ~/.config/i3/config so your lines win.
#
# Examples:
#   bindsym $mod+Shift+x exec --no-startup-id firefox
#   for_window [class="^Gimp$"] floating enable
#
# Extra keybindings: prefer ~/.config/up/keybindings-overrides.toml
# (merged into keybindings.conf on generate).
